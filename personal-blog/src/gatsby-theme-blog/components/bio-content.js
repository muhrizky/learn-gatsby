import React, { Fragment } from "react"
import { Styled } from "theme-ui"

/**
 * Change the content to add your own bio
 */

export default () => (
  <Fragment>
    This is my update bio
    <br />
    It's shodowing the content from the theme.
  </Fragment>
)
